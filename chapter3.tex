% !TeX root = book.tex
\section{Thiết kế cơ sở dữ liệu mức khái niệm}\label{s:conceptual}

Phần này trình bày mô hình cơ sở dữ liệu mức khái niệm cho nền tảng quản lý giao hàng bằng máy bay không người lái có tích hợp trí tuệ nhân tạo. Mô hình xác định các thực thể chính, thuộc tính của chúng, mối quan hệ giữa các thực thể và bản số của những mối quan hệ đó. Đây là cơ sở để xây dựng lược đồ quan hệ mức logic trong Phần~\ref{s:logical}.

\subsection{Vì sao mức khái niệm phải được xác định trước}

Trong nền tảng quản lý giao hàng bằng máy bay không người lái có tích hợp trí tuệ nhân tạo, mức khái niệm xác định được:
\begin{itemize}
    \item Phạm vi nghiệp vụ của nền tảng và ranh giới với các hệ thống/thiết bị bên ngoài.
    \item Tác nhân, trách nhiệm của từng tác nhân và điểm họ tương tác với hệ thống.
    \item Các thực thể nghiệp vụ cốt lõi và ý nghĩa nghiệp vụ của từng thực thể.
    \item Mối quan hệ giữa các thực thể cùng lực lượng số và tính bắt buộc/tùy chọn khi phù hợp.
    \item Các vòng đời và sự kiện cần lưu lại, đặc biệt là trạng thái, dữ liệu theo dõi, sự cố và việc xử lý tại trạm.
    \item Các quy tắc nghiệp vụ phải luôn đúng dù giao diện hay dịch vụ ứng dụng thay đổi.
\end{itemize}

\subsection{Từ việc hiểu nghiệp vụ đến mô hình khái niệm}

Chuỗi chuyển đổi từ bài toán kinh doanh sang thiết kế:

\begin{center}
Nhu cầu nghiệp vụ $\to$ Tác nhân $\to$ Quy trình/sự kiện nghiệp vụ $\to$ Thực thể nghiệp vụ\\
$\to$ Mối quan hệ/lực lượng số $\to$ Mô hình khái niệm
\end{center}

Ví dụ: yêu cầu ``hệ thống phải hỗ trợ giao lại khi một lần giao thất bại'' dẫn đến việc nhận diện hai khái niệm khác nhau: Đơn giao hàng (\code{DELIVERY\_ORDER}) và Lần thực hiện giao hàng (\code{DELIVERY\_ACTIVITY}).

Yêu cầu chức năng và quy tắc nghiệp vụ là nguồn đầu vào của mức khái niệm, nhưng chúng không đồng nghĩa với khái niệm. Yêu cầu chức năng mô tả hệ thống phải làm gì; mô hình khái niệm mô tả hệ thống cần biết gì.

\subsection{Ranh giới hệ thống và kịch bản nghiệp vụ}

Nền tảng quản lý giao hàng bằng máy bay không người lái có tích hợp trí tuệ nhân tạo được xác định là nền tảng quản lý nghiệp vụ và logistics cho giao hàng bằng drone. Phạm vi gồm khách hàng, địa chỉ, đơn giao hàng, gói hàng, trạm hạ cánh, drone, lần thực hiện giao hàng, dữ liệu theo dõi, sự cố, xác nhận, lộ trình, khuyến nghị \code{AI}, phân quyền và kiểm toán. Điều khiển bay mức thấp, phần sụn, phần cứng động cơ và chi tiết bên trong kho lưu trữ đối tượng không phải là các thực thể nghiệp vụ cốt lõi của cơ sở dữ liệu này.

Một kịch bản nghiệp vụ dùng để kiểm tra mức khái niệm có thể diễn ra như sau:
\begin{itemize}
    \item Khách hàng chọn một địa chỉ và tạo đơn giao hàng.
    \item Một đơn giao hàng có thể chứa một hoặc nhiều gói hàng.
    \item Điều phối viên xử lý đơn và tạo một lần thực hiện giao hàng để thực hiện việc giao.
    \item Lần thực hiện được gán một drone và các trạm liên quan; nhân viên vận hành xử lý gói hàng tại trạm.
    \item Trong khi bay, nhiều bản ghi theo dõi được sinh ra; nếu có sự cố thì tạo bản ghi sự cố giao hàng.
    \item Nếu lần thực hiện thất bại, một lần thực hiện mới được tạo và liên kết với lần trước để bảo toàn lịch sử lập lịch lại.
    \item Khi giao thành công, xác nhận giao hàng ghi nhận kết quả và tham chiếu bằng chứng.
\end{itemize}

\subsection{Xác định thực thể: mỗi khái niệm có ý nghĩa gì} Một thực thể chỉ nên được đưa vào mô hình khi nó đại diện cho một đối tượng nghiệp vụ độc lập, một vòng đời riêng, một loại sự kiện lặp lại hoặc một mối quan hệ cần được quản lý riêng. \renewcommand{\arraystretch}{1.15} \small \begin{longtable}{|p{3.5cm}|p{4.5cm}|p{7cm}|} \hline \textbf{Nhóm khái niệm} & \textbf{Thực thể / Quan hệ} & \textbf{Ý nghĩa nghiệp vụ} \\ \hline \endfirsthead \hline \textbf{Nhóm khái niệm} & \textbf{Thực thể / Quan hệ} & \textbf{Ý nghĩa nghiệp vụ} \\ \hline \endhead Định danh và khách hàng & \texttt{SYSTEM\_USER}, \texttt{CUSTOMER}, \texttt{CUSTOMER\_ADDRESS} & Quản lý tài khoản, hồ sơ khách hàng và các địa chỉ có thể được sử dụng làm điểm giao hàng. \\ \hline Đơn giao hàng và kiện hàng & \texttt{DELIVERY\_ORDER}, \texttt{PACKAGE} & Mô tả yêu cầu giao hàng và các đơn vị logistics thuộc đơn hàng. \\ \hline Trạm hạ cánh và drone & \texttt{LANDING\_STATION}, \texttt{DRONE} & Đại diện cho hạ tầng và phương tiện thực hiện hoạt động giao hàng. \\ \hline Thực hiện giao hàng & \texttt{DELIVERY\_ACTIVITY} & Đại diện cho một lần thực hiện (attempt) cụ thể của đơn giao hàng; là thực thể trung tâm của mô hình nghiệp vụ. \\ \hline Lịch sử và sự kiện & \texttt{ORDER\_STATUS\_HISTORY}, \texttt{STATION\_STATUS\_HISTORY}, \texttt{PACKAGE\_STATION\_EVENT}, \texttt{TRACKING\_RECORD}, \texttt{DELIVERY\_EXCEPTION} & Lưu các sự kiện phát sinh theo thời gian; tránh lưu danh sách lặp hoặc dữ liệu lịch sử trực tiếp trong thực thể cha. \\ \hline Xác nhận giao hàng & \texttt{DELIVERY\_CONFIRMATION} & Lưu kết quả giao hàng thành công và tham chiếu tới bằng chứng xác nhận. \\ \hline Lộ trình và trí tuệ nhân tạo & \texttt{DELIVERY\_ROUTE}, \texttt{ROUTE\_POINT}, \texttt{AI\_RECOMMENDATION} & Quản lý lộ trình dự kiến, các điểm hành trình và các khuyến nghị từ mô hình AI. \\ \hline Phân quyền và kiểm toán & \texttt{ROLE}, \texttt{PERMISSION}, \texttt{USER\_ROLE}, \texttt{ROLE\_PERMISSION}, \texttt{AUDIT\_LOG} & Hỗ trợ kiểm soát truy cập, quản lý quyền hạn và truy vết các thao tác trong hệ thống. \\ \hline \end{longtable} \normalsize

\renewcommand{\arraystretch}{1.2} \small \begin{longtable}{|p{5cm}|p{10.45cm}|} \hline \textbf{Thực thể} & \textbf{Giải thích} \\ \hline \endfirsthead \hline \textbf{Thực thể} & \textbf{Giải thích} \\ \hline \endhead CUSTOMER và CUSTOMER\_ADDRESS & CUSTOMER đại diện cho chủ thể sử dụng dịch vụ. CUSTOMER\_ADDRESS không chỉ là một thuộc tính văn bản của khách hàng vì một khách hàng có thể có nhiều địa chỉ và mỗi địa chỉ có tọa độ phục vụ giao hàng. Do đó địa chỉ là một thực thể độc lập có vòng đời riêng. \\ \hline DELIVERY\_ORDER & Đại diện cho yêu cầu nghiệp vụ giao hàng. Thực thể này lưu thông tin khách hàng, địa chỉ giao, trạng thái đơn hàng, thời gian yêu cầu, ETA và kết quả hoàn thành hoặc hủy. Không chứa dữ liệu theo dõi hay lịch sử thực hiện. \\ \hline PACKAGE & Đại diện cho đơn vị logistics vật lý thuộc một đơn hàng. Một đơn hàng có thể chứa nhiều kiện hàng và mỗi kiện hàng có các thuộc tính riêng như khối lượng, kích thước và trạng thái xử lý. \\ \hline LANDING\_STATION và DRONE & LANDING\_STATION là hạ tầng vận hành, còn DRONE là nguồn lực giao hàng. Hai thực thể này tồn tại độc lập với đơn hàng, có vòng đời riêng và được tái sử dụng cho nhiều lần giao hàng khác nhau. \\ \hline DELIVERY\_ACTIVITY & Mô tả một lần thực hiện giao hàng cụ thể. Đây là thực thể trung tâm liên kết drone, trạm lấy/trả, lịch thực hiện, dữ liệu theo dõi, lộ trình, sự cố và các khuyến nghị AI. \\ \hline Các thực thể lịch sử/sự kiện & Các bảng lịch sử và sự kiện được tách riêng khi thông tin thay đổi theo thời gian hoặc phát sinh nhiều lần. Ví dụ: lịch sử trạng thái đơn hàng, lịch sử trạng thái trạm, sự kiện xử lý kiện hàng, dữ liệu theo dõi và sự cố phát sinh. \\ \hline DELIVERY\_CONFIRMATION & Lưu kết quả xác nhận giao hàng thành công. Các bằng chứng xác nhận được tham chiếu thông qua khóa hoặc đường dẫn lưu trữ thay vì nhúng trực tiếp dữ liệu tệp vào cơ sở dữ liệu. \\ \hline Phân quyền và kiểm toán & Người dùng, vai trò và quyền được mô hình hóa thành các thực thể riêng biệt với quan hệ nhiều-nhiều. Nhật ký kiểm toán được lưu độc lập nhằm phục vụ quản trị, bảo mật và truy vết hoạt động hệ thống. \\ \hline \end{longtable}
\subsection{Các mối quan hệ cốt lõi}  \renewcommand{\arraystretch}{1.15} \small \begin{longtable}{|p{4cm}|p{1cm}|p{5.0cm}|p{5.5cm}|} \hline \textbf{Mối quan hệ} & \textbf{Bản số} & \textbf{Diễn giải nghiệp vụ} & \textbf{Hệ quả cho thiết kế logic} \\ \hline \endfirsthead \hline \textbf{Mối quan hệ} & \textbf{Bản số} & \textbf{Diễn giải nghiệp vụ} & \textbf{Hệ quả cho thiết kế logic} \\ \hline \endhead \texttt{CUSTOMER} -- \texttt{CUSTOMER\_ADDRESS} & 1:N & Một khách hàng có thể sở hữu nhiều địa chỉ giao hàng. & Tách bảng \texttt{CUSTOMER\_ADDRESS}; sử dụng \texttt{customer\_id} làm khóa ngoại. \\ \hline \texttt{CUSTOMER} -- \texttt{DELIVERY\_ORDER} & 1:N & Một khách hàng có thể tạo nhiều đơn giao hàng. & \texttt{customer\_id} trở thành khóa ngoại trong \texttt{DELIVERY\_ORDER}. \\ \hline \texttt{DELIVERY\_ORDER} -- \texttt{PACKAGE} & 1:N & Một đơn giao hàng gồm một hoặc nhiều kiện hàng. & \texttt{PACKAGE} trở thành thực thể riêng. \\ \hline \texttt{DELIVERY\_ORDER} -- \texttt{DELIVERY\_ACTIVITY} & 1:N & Một đơn hàng có thể trải qua nhiều lần thực hiện do giao lại hoặc lập lịch lại. & Tách yêu cầu giao hàng khỏi quá trình thực hiện. \\ \hline \texttt{DRONE} -- \texttt{DELIVERY\_ACTIVITY} & 1:N & Một drone thực hiện nhiều chuyến giao hàng theo thời gian. & Drone và hoạt động giao hàng là hai thực thể độc lập. \\ \hline \texttt{DELIVERY\_ACTIVITY} -- \texttt{TRACKING\_RECORD} & 1:N & Một lần thực hiện sinh ra nhiều bản ghi đo từ xa. & \texttt{TRACKING\_RECORD} là bảng sự kiện (event relation). \\ \hline \texttt{DELIVERY\_ACTIVITY} -- \texttt{DELIVERY\_EXCEPTION} & 1:N & Một lần thực hiện có thể gặp nhiều sự cố hoặc không có sự cố nào. & Sự cố được mô hình hóa thành thực thể độc lập. \\ \hline \texttt{PACKAGE} -- \texttt{PACKAGE\_STATION\_EVENT} & 1:N & Một kiện hàng trải qua nhiều sự kiện tại các trạm. & Tránh lưu nhiều cột như \texttt{station\_1}, \texttt{station\_2}, ... \\ \hline \texttt{DELIVERY\_ACTIVITY} -- \texttt{DELIVERY\_ROUTE} & 1:N & Một hoạt động có thể có nhiều phiên bản lộ trình. & Nếu chỉ cho phép một lộ trình đang hoạt động cần bổ sung ràng buộc nghiệp vụ. \\ \hline \texttt{DELIVERY\_ROUTE} -- \texttt{ROUTE\_POINT} & 1:N & Một lộ trình bao gồm nhiều điểm hành trình có thứ tự. & Tách \texttt{ROUTE\_POINT} khỏi \texttt{DELIVERY\_ROUTE}. \\ \hline \texttt{DELIVERY\_ORDER} -- \texttt{DELIVERY\_CONFIRMATION} & 1:0..1 & Đơn hàng chỉ có xác nhận khi giao thành công. & Ràng buộc \texttt{UNIQUE(order\_id)} hỗ trợ quy tắc này. \\ \hline \texttt{SYSTEM\_USER} -- \texttt{ROLE} & N:M & Một người dùng có thể có nhiều vai trò và ngược lại. & Triển khai bằng bảng liên kết \texttt{USER\_ROLE}. \\ \hline \texttt{ROLE} -- \texttt{PERMISSION} & N:M & Một vai trò gồm nhiều quyền và một quyền có thể thuộc nhiều vai trò. & Triển khai bằng bảng liên kết \texttt{ROLE\_PERMISSION}. \\ \hline \end{longtable} \normalsize

\subsection{Quyết định khái niệm quan trọng nhất: Đơn giao hàng và Lần thực hiện giao hàng}

Đây là quyết định quan trọng nhất của mô hình khái niệm hiện tại. \code{DELIVERY\_ORDER} và \code{DELIVERY\_ACTIVITY} không phải hai tên gọi của cùng một thứ:
\begin{itemize}
    \item \code{DELIVERY\_ORDER} = ``khách hàng yêu cầu hệ thống thực hiện một lần giao''.
    \item \code{DELIVERY\_ACTIVITY} = ``một lần thực hiện cụ thể mà hệ thống thực sự tổ chức để thực hiện yêu cầu đó''.
    \item Một order có thể có nhiều activity; mỗi activity có drone, thời gian, trạm, dữ liệu theo dõi và sự cố riêng.
    \item \code{previous\_activity\_id} tạo chuỗi liên kết khi lần thực hiện sau được sinh ra do lập lịch lại.
\end{itemize}

Ví dụ: O100 là một yêu cầu. A1 được giao cho D3 nhưng thất bại. Điều phối viên tạo A2 và \code{A2.previous\_activity\_id = A1}. Cơ sở dữ liệu giữ một O100 nhưng hai activity với lịch sử đầy đủ.

\subsection{Các khái niệm về vòng đời và sự kiện}

\paragraph{Trạng thái hiện tại và lịch sử.}
\code{DELIVERY\_ORDER} và \code{LANDING\_STATION} có trạng thái hiện tại để đọc nhanh. \code{ORDER\_STATUS\_HISTORY} và \code{STATION\_STATUS\_HISTORY} lưu các lần chuyển trạng thái để kiểm toán.

\paragraph{Di chuyển của gói hàng.}
\code{PACKAGE\_STATION\_EVENT} ghi mỗi lần package được xử lý tại một station cùng operator, loại sự kiện và thời điểm.

\paragraph{Dữ liệu đo từ xa.}
\code{TRACKING\_RECORD} là chuỗi quan sát theo thời gian của một lần thực hiện giao hàng; mỗi dòng là một quan sát thay vì nhiều cột \code{coordinate\_1}, \code{coordinate\_2}, \ldots

\paragraph{Sự cố.}
\code{DELIVERY\_EXCEPTION} ghi sự cố phát sinh trong một activity, có thời điểm xảy ra và trạng thái xử lý.

\paragraph{Xác nhận.}
\code{DELIVERY\_CONFIRMATION} ghi sự kiện giao thành công; \code{evidence\_object\_key} là tham chiếu đến bằng chứng bên ngoài.

\subsection{Danh sách kiểm tra mô hình khái niệm}

\begin{itemize}
    \item Mỗi đối tượng nghiệp vụ cốt lõi có ý nghĩa rõ ràng và không bị chồng lấn trách nhiệm.
    \item Mỗi mối quan hệ quan trọng có lực lượng số rõ ràng.
    \item Các dữ liệu lặp theo thời gian đã được nhận diện thành lịch sử/sự kiện.
    \item Các vòng đời độc lập được tách ra thay vì gắn tất cả vào một thực thể.
    \item Mức khái niệm biểu diễn được các tình huống bình thường, thất bại, thử lại, hủy và hoàn thành.
    \item Mức khái niệm có thể dẫn tới lược đồ logic mà không phải thay đổi ý nghĩa nghiệp vụ.
    \item Mức khái niệm không kéo các thành phần ngoài phạm vi vào thành thực thể nghiệp vụ cốt lõi.
    \item Mức khái niệm hỗ trợ các câu hỏi nghiệp vụ cốt lõi của hệ thống.
\end{itemize}

\subsection{Đối chiếu từ khái niệm đến vấn đề nghiệp vụ} \renewcommand{\arraystretch}{1.15} \small \begin{longtable}{|p{3cm}|p{3.4cm}|p{4cm}|p{4.1cm}|} \hline \textbf{Vấn đề nghiệp vụ} & \textbf{Diễn giải khái niệm} & \textbf{Thực thể / Mối quan hệ} & \textbf{Ý nghĩa thiết kế} \\ \hline \endfirsthead \hline \textbf{Vấn đề nghiệp vụ} & \textbf{Diễn giải khái niệm} & \textbf{Thực thể / Mối quan hệ} & \textbf{Ý nghĩa thiết kế} \\ \hline \endhead Đơn giao hàng có thể giao lại & Yêu cầu giao hàng và lần thực hiện là hai khái niệm khác nhau. & \texttt{DELIVERY\_ORDER} (1:N) \texttt{DELIVERY\_ACTIVITY} & Cho phép retry, reschedule và theo dõi chuỗi thực hiện. \\ \hline Cần kiểm toán vòng đời & Trạng thái hiện tại khác với lịch sử thay đổi trạng thái. & \texttt{DELIVERY\_ORDER}, \texttt{ORDER\_STATUS\_HISTORY} & Dẫn đến việc tách bảng lịch sử trạng thái. \\ \hline Kiện hàng đi qua nhiều trạm & Xử lý kiện hàng tại trạm là chuỗi các sự kiện. & \texttt{PACKAGE} (1:N) \texttt{PACKAGE\_STATION\_EVENT} & Dẫn đến mô hình hóa theo event history. \\ \hline Theo dõi drone theo từng chuyến & Dữ liệu đo từ xa thuộc về một lần thực hiện cụ thể. & \texttt{DELIVERY\_ACTIVITY} (1:N) \texttt{TRACKING\_RECORD} & Tránh trộn telemetry giữa các lần giao hàng. \\ \hline Giao hàng có thể phát sinh sự cố & Sự cố có thời điểm phát sinh và xử lý riêng. & \texttt{DELIVERY\_ACTIVITY} (1:N) \texttt{DELIVERY\_EXCEPTION} & Hỗ trợ thống kê và phân tích nguyên nhân thất bại. \\ \hline Phân quyền có quan hệ N:N & Vai trò và quyền là các khái niệm độc lập. & \texttt{USER\_ROLE}, \texttt{ROLE\_PERMISSION} & Dẫn đến các bảng liên kết (associative entities). \\ \hline Cần bằng chứng giao hàng & Xác nhận giao hàng là một sự kiện nghiệp vụ độc lập. & \texttt{DELIVERY\_ORDER} (1:0..1) \texttt{DELIVERY\_CONFIRMATION} & Tách dữ liệu xác nhận khỏi kho lưu trữ tệp. \\ \hline \end{longtable} \normalsize

\subsection{Từ mô hình khái niệm đến thiết kế logic}

Phần này chuyển các thành phần của mô hình khái niệm thành lược đồ quan hệ mức logic, bao gồm quan hệ, khóa và các ràng buộc toàn vẹn, nhưng chưa phụ thuộc vào cách cài đặt vật lý cụ thể.

Chuỗi các bước thiết kế:
\begin{enumerate}
    \item Chuyển mỗi thực thể độc lập thành một quan hệ và lựa chọn khóa chính.
    \item Chuyển các thuộc tính thành cột; tách thuộc tính đa trị hoặc lặp thành quan hệ riêng.
    \item Biểu diễn quan hệ 1:N bằng khóa ngoại và quan hệ N:M bằng bảng liên kết.
    \item Xác định khóa ngoại, ràng buộc toàn vẹn và các quy tắc chuyển trạng thái.
    \item Kiểm tra phụ thuộc hàm, chuẩn hóa và hoàn thiện lược đồ logic.
\end{enumerate}

\subsection{Mối quan hệ và bản số}\label{s:relationships}

Các mối quan hệ chính giữa các thực thể được trình bày trong bảng sau:

\renewcommand{\arraystretch}{1.2}
\begin{longtable}{|p{3.2cm}|p{1.5cm}|p{3.5cm}|p{6cm}|}
\hline
\textbf{Thực thể A} &
\textbf{Bản số} &
\textbf{Thực thể B} &
\textbf{Ý nghĩa nghiệp vụ} \\ \hline
\endfirsthead

\hline
\textbf{Thực thể A} &
\textbf{Bản số} &
\textbf{Thực thể B} &
\textbf{Ý nghĩa nghiệp vụ} \\ \hline
\endhead

\texttt{customer} &
1:N &
\texttt{customer\_address} &
Một khách hàng có thể có nhiều địa chỉ giao hàng. \\ \hline

\texttt{customer} &
1:N &
\texttt{delivery\_order} &
Một khách hàng có thể tạo nhiều đơn giao hàng. \\ \hline

\texttt{delivery\_order} &
1:N &
\texttt{package} &
Một đơn giao hàng có thể chứa nhiều kiện hàng. \\ \hline

\texttt{delivery\_order} &
1:N &
\texttt{order\_status\_history} &
Một đơn hàng có nhiều bản ghi lịch sử trạng thái. \\ \hline

\texttt{delivery\_order} &
1:N &
\texttt{delivery\_activity} &
Một đơn hàng có thể được thực hiện nhiều lần khi giao lại hoặc lập lịch lại. \\ \hline

\texttt{drone} &
1:N &
\texttt{delivery\_activity} &
Một máy bay có thể thực hiện nhiều hoạt động giao hàng theo thời gian. \\ \hline

\texttt{landing\_station} &
1:N &
\texttt{delivery\_activity} &
Một trạm có thể là trạm khởi hành hoặc trạm đích của nhiều hoạt động giao hàng. \\ \hline

\texttt{delivery\_activity} &
1:N &
\texttt{tracking\_record} &
Một lần thực hiện giao hàng sinh ra nhiều bản ghi theo dõi. \\ \hline

\texttt{delivery\_activity} &
1:N &
\texttt{delivery\_exception} &
Một lần giao hàng có thể phát sinh nhiều sự cố hoặc ngoại lệ. \\ \hline

\texttt{package} &
1:N &
\texttt{package\_station\_event} &
Một kiện hàng có thể trải qua nhiều sự kiện tại các trạm. \\ \hline

\texttt{delivery\_order} &
1:0..1 &
\texttt{delivery\_confirmation} &
Một đơn hàng có tối đa một xác nhận giao hàng thành công. \\ \hline

\texttt{system\_user} &
N:M &
\texttt{role} &
Quan hệ nhiều--nhiều được triển khai qua bảng \texttt{user\_role}. \\ \hline

\texttt{role} &
N:M &
\texttt{permission} &
Quan hệ nhiều--nhiều được triển khai qua bảng \texttt{role\_permission}. \\ \hline

\texttt{delivery\_route} &
1:N &
\texttt{route\_point} &
Một tuyến đường gồm nhiều điểm hành trình có thứ tự. \\ \hline

\texttt{delivery\_order} &
1:N &
\texttt{ai\_recommendation} &
Một đơn hàng có thể có nhiều kết quả đề xuất từ mô hình AI. \\ \hline

\end{longtable}
\renewcommand{\arraystretch}{1}

\section{Thiết kế cơ sở dữ liệu mức logic}\label{s:logical}

Phần này xây dựng lược đồ quan hệ mức logic từ mô hình mức khái niệm. Lược đồ quan hệ được trình bày theo ký hiệu chuẩn, trong đó khóa chính được ký hiệu là (PK), khóa ngoại là (FK) và ràng buộc duy nhất là UNIQUE.

\subsection{Lược đồ quan hệ}

\begin{verbatim}
app_user(user_id PK, username UNIQUE, password_hash,
  email UNIQUE, phone_number UNIQUE, is_active,
  created_at, updated_at)
\end{verbatim}
\subsection{Xác định thực thể}\label{s:entities} Dựa trên các yêu cầu và quy tắc nghiệp vụ được trình bày tại Chương~\ref{c:analysis}, hai mươi ba thực thể được xác định. Các thực thể được tổ chức thành bảy nhóm chức năng nhằm bảo đảm bao phủ đầy đủ nhưng không tạo ra dư thừa trong vận hành. \renewcommand{\arraystretch}{1.15} \small \begin{longtable}{|p{1cm}|p{4cm}|p{3.5cm}|p{7cm}|} \hline \textbf{STT} & \textbf{Nhóm chức năng} & \textbf{Thực thể} & \textbf{Mô tả} \\ \hline \endfirsthead \hline \textbf{STT} & \textbf{Nhóm chức năng} & \textbf{Thực thể} & \textbf{Mô tả} \\ \hline \endhead 1 & Người dùng và phân quyền & \texttt{system\_user} & Lưu thông tin xác thực và trạng thái tài khoản của mọi tác nhân trong hệ thống. \\ \hline 2 & Người dùng và phân quyền & \texttt{role} & Xác định các vai trò vận hành như khách hàng, điều phối viên, nhân viên trạm và quản trị viên. \\ \hline 3 & Người dùng và phân quyền & \texttt{permission} & Xác định các quyền thao tác chi tiết trong hệ thống. \\ \hline 4 & Người dùng và phân quyền & \texttt{user\_role} & Bảng liên kết triển khai quan hệ nhiều-nhiều giữa người dùng và vai trò. \\ \hline 5 & Người dùng và phân quyền & \texttt{role\_permission} & Bảng liên kết triển khai quan hệ nhiều-nhiều giữa vai trò và quyền hạn. \\ \hline 6 & Khách hàng và địa chỉ & \texttt{customer} & Lưu hồ sơ khách hàng và liên kết với tài khoản người dùng nếu có. \\ \hline 7 & Khách hàng và địa chỉ & \texttt{customer\_address} & Lưu nhiều địa chỉ giao hàng của khách hàng kèm tọa độ địa lý. \\ \hline 8 & Đơn hàng và kiện hàng & \texttt{delivery\_order} & Đại diện cho yêu cầu giao hàng của khách hàng và vòng đời đơn hàng. \\ \hline 9 & Đơn hàng và kiện hàng & \texttt{package} & Lưu thông tin vật lý của các kiện hàng thuộc đơn giao hàng. \\ \hline 10 & Đơn hàng và kiện hàng & \texttt{order\_status\_history} & Lưu lịch sử thay đổi trạng thái đơn hàng dưới dạng chỉ ghi nối tiếp. \\ \hline 11 & Trạm hạ cánh và máy bay & \texttt{landing\_station} & Lưu thông tin các trạm cất và hạ cánh của máy bay. \\ \hline 12 & Trạm hạ cánh và máy bay & \texttt{station\_status\_history} & Lưu lịch sử thay đổi trạng thái hoạt động của trạm. \\ \hline 13 & Trạm hạ cánh và máy bay & \texttt{station\_operator\_assignment} & Lưu thông tin phân công nhân viên vận hành tại các trạm. \\ \hline 14 & Trạm hạ cánh và máy bay & \texttt{drone} & Lưu thông tin nhận dạng và tình trạng vận hành của drone. \\ \hline 15 & Thực hiện giao hàng & \texttt{delivery\_activity} & Đại diện cho một lần thực hiện giao hàng cụ thể. \\ \hline 16 & Thực hiện giao hàng & \texttt{package\_station\_event} & Lưu các sự kiện xử lý kiện hàng tại trạm. \\ \hline 17 & Thực hiện giao hàng & \texttt{tracking\_record} & Lưu dữ liệu GPS và dữ liệu đo từ xa trong chuyến bay. \\ \hline 18 & Thực hiện giao hàng & \texttt{delivery\_exception} & Lưu các sự cố, thất bại hoặc bất thường trong quá trình giao hàng. \\ \hline 19 & Xác nhận giao hàng & \texttt{delivery\_confirmation} & Lưu bằng chứng giao hàng thành công và thông tin người nhận. \\ \hline 20 & Kiểm toán, tuyến đường và AI & \texttt{audit\_log} & Lưu nhật ký kiểm toán và truy vết các thao tác trong hệ thống. \\ \hline 21 & Kiểm toán, tuyến đường và AI & \texttt{delivery\_route} & Lưu siêu dữ liệu của tuyến đường giao hàng dự kiến. \\ \hline 22 & Kiểm toán, tuyến đường và AI & \texttt{route\_point} & Lưu danh sách các điểm hành trình thuộc một tuyến đường. \\ \hline 23 & Kiểm toán, tuyến đường và AI & \texttt{ai\_recommendation} & Lưu kết quả đề xuất từ các mô hình trí tuệ nhân tạo như drone phù hợp hoặc ETA dự kiến. \\ \hline \end{longtable}

\subsection{Thuộc tính của thực thể}\label{s:attributes}

Hình dưới đây trình bày tổng thể các thực thể, thuộc tính, khóa chính, khóa ngoại và các ràng buộc của cơ sở dữ liệu quản lý giao hàng bằng máy bay không người lái có tích hợp trí tuệ nhân tạo.

\begin{figure}[H]
    \centering
    \includegraphics[
        width=\textwidth,
        height=0.72\textheight,
        keepaspectratio
    ]{erd.png}
    \caption{Mô hình quan hệ thực thể}
    \label{fig:entity-attributes}
\end{figure}


Phần này xây dựng lược đồ quan hệ mức logic từ mô hình mức khái niệm. Lược đồ quan hệ được trình bày theo ký hiệu chuẩn, trong đó khóa chính được ký hiệu là (PK), khóa ngoại là (FK) và ràng buộc duy nhất là UNIQUE.

\subsection{Lược đồ quan hệ} \small \renewcommand{\arraystretch}{1.1} \begin{longtable}{|>{\raggedright\arraybackslash}p{5.5cm}| >{\raggedright\arraybackslash}p{9cm}|} \hline \textbf{Quan hệ} & \textbf{Thuộc tính} \\ \hline \endfirsthead \hline \textbf{Quan hệ} & \textbf{Thuộc tính} \\ \hline \endhead SYSTEM\_USER & \underline{user\_id}, username, password\_hash, email, phone\_number, is\_active, created\_at, updated\_at \\ \hline ROLE & \underline{role\_id}, role\_name, description \\ \hline PERMISSION & \underline{permission\_id}, permission\_name, description \\ \hline USER\_ROLE & \underline{user\_id}, \underline{role\_id}, assigned\_at \\ \hline ROLE\_PERMISSION & \underline{role\_id}, \underline{permission\_id} \\ \hline CUSTOMER & \underline{customer\_id}, user\_id, customer\_name, phone\_number, email, created\_at \\ \hline CUSTOMER\_ADDRESS & \underline{address\_id}, customer\_id, address\_label, address\_line, ward, district, city, latitude, longitude, is\_default, created\_at \\ \hline DELIVERY\_ORDER & \underline{order\_id}, customer\_id, delivery\_address\_id, order\_status, requested\_delivery\_time, estimated\_arrival\_at, created\_at, completed\_at, cancelled\_at, cancellation\_reason \\ \hline PACKAGE & \underline{package\_id}, order\_id, description, weight\_kg, length\_cm, width\_cm, height\_cm, package\_status, created\_at \\ \hline ORDER\_STATUS\_HISTORY & \underline{status\_history\_id}, order\_id, status, changed\_by, reason, changed\_at \\ \hline LANDING\_STATION & \underline{station\_id}, station\_code, station\_name, address, latitude, longitude, capacity, status, created\_at \\ \hline STATION\_STATUS\_HISTORY & \underline{history\_id}, station\_id, status, changed\_by, reason, changed\_at \\ \hline STATION\_OPERATOR\_ASSIGNMENT & \underline{assignment\_id}, station\_id, user\_id, assigned\_from, assigned\_to, is\_active, created\_at \\ \hline DRONE & \underline{drone\_id}, serial\_number, model, status, max\_payload\_kg, battery\_level, station\_id, created\_at \\ \hline DELIVERY\_ACTIVITY & \underline{activity\_id}, order\_id, drone\_id, pickup\_station\_id, destination\_station\_id, assigned\_by, scheduled\_at, activity\_status, started\_at, ended\_at, previous\_activity\_id, created\_at \\ \hline PACKAGE\_STATION\_EVENT & \underline{event\_id}, package\_id, station\_id, event\_type, performed\_by, occurred\_at, note \\ \hline TRACKING\_RECORD & \underline{tracking\_id}, activity\_id, latitude, longitude, altitude\_m, speed\_kmh, battery\_level, recorded\_at \\ \hline DELIVERY\_CONFIRMATION & \underline{confirmation\_id}, order\_id, activity\_id, confirmed\_by, receiver\_name, receiver\_phone, confirmation\_method, evidence\_object\_key, confirmed\_at, note \\ \hline DELIVERY\_EXCEPTION & \underline{exception\_id}, activity\_id, exception\_type, description, detected\_at, resolved\_at, resolved\_by, resolution \\ \hline AUDIT\_LOG & \underline{log\_id}, user\_id, entity\_name, record\_id, action\_type, old\_value, new\_value, action\_time, ip\_address \\ \hline DELIVERY\_ROUTE & \underline{route\_id}, activity\_id, total\_distance\_km, estimated\_duration\_min, created\_at \\ \hline ROUTE\_POINT & \underline{route\_point\_id}, route\_id, sequence\_number, latitude, longitude, altitude\_m \\ \hline AI\_RECOMMENDATION & \underline{recommendation\_id}, order\_id, activity\_id, recommendation\_type, recommended\_drone\_id, estimated\_duration\_min, confidence\_score, model\_version, created\_at \\ \hline \end{longtable} \textit{Ghi chú: Thuộc tính được gạch chân là khóa chính (PK). Các thuộc tính tham chiếu tới quan hệ khác là khóa ngoại (FK).}
\subsection{Khóa chính, khóa ngoại và khóa ứng viên}

Mỗi bảng cơ sở có một khóa chính thay thế gồm một cột (\texttt{BIGINT GENERATED ALWAYS AS IDENTITY}). Các bảng liên kết (\texttt{user\_role}, \texttt{role\_permission}) sử dụng khóa chính ghép. Các khóa tự nhiên ứng viên được bảo vệ bằng ràng buộc \texttt{UNIQUE}:
\begin{itemize}
    \item \texttt{system\_user.username}, \texttt{system\_user.email}, \texttt{system\_user.phone\_number}
    \item \texttt{customer.phone\_number}, \texttt{customer.email}
    \item \texttt{drone.serial\_number}
    \item \texttt{landing\_station.station\_code}
    \item \texttt{delivery\_confirmation.order\_id} và \texttt{delivery\_confirmation.activity\_id}
    \item \texttt{delivery\_route.activity\_id}
    \item \texttt{route\_point(route\_id, sequence\_number)}
\end{itemize}

\subsection{Phụ thuộc hàm và chuẩn hóa}

Cả hai mươi ba quan hệ đều thỏa mãn Dạng chuẩn thứ ba (3NF):

\paragraph{Dạng chuẩn thứ nhất (1NF).}
Mọi giá trị cột đều có tính nguyên tử. Các thành phần địa chỉ được tách thành những thuộc tính riêng; kích thước được lưu dưới dạng các giá trị số độc lập (\texttt{length\_cm}, \texttt{width\_cm}, \texttt{height\_cm}); nhiều địa chỉ hoặc kiện hàng được biểu diễn bằng các dòng quan hệ riêng thay vì chuỗi phức hợp.

\paragraph{Dạng chuẩn thứ hai (2NF).}
Mỗi thuộc tính không khóa phụ thuộc hàm đầy đủ vào toàn bộ khóa chính. Trong các bảng có khóa ghép (\texttt{user\_role}, \texttt{role\_permission}), thuộc tính không khóa phụ thuộc vào cả hai thành phần của khóa.

\paragraph{Dạng chuẩn thứ ba (3NF).}
Không tồn tại phụ thuộc bắc cầu giữa các thuộc tính không khóa. Ví dụ, \texttt{delivery\_order} tham chiếu \texttt{customer\_id} và không lưu lại thông tin liên hệ của khách hàng; \texttt{delivery\_confirmation} không lưu dữ liệu khách hàng hoặc máy bay; còn \texttt{tracking\_record} tham chiếu \texttt{activity\_id} mà không lưu dư thừa thuộc tính của máy bay.

\subsection{Các ràng buộc toàn vẹn}

Lược đồ quan hệ thực thi sáu nhóm ràng buộc toàn vẹn:

\begin{enumerate}
    \item \textbf{Toàn vẹn thực thể}: Được bảo đảm bởi các khóa chính thay thế tự động sinh, không rỗng và duy nhất trong từng bảng.
    \item \textbf{Toàn vẹn tham chiếu}: Được thực thi bằng các ràng buộc khóa ngoại. Dữ liệu chủ phục vụ vận hành (\texttt{customer}, \texttt{drone}, \texttt{landing\_station}) sử dụng \texttt{ON DELETE RESTRICT} để ngăn vô tình xóa lịch sử vận hành. Các bảng liên kết bảo mật sử dụng \texttt{ON DELETE CASCADE}. Tham chiếu người dùng trong nhật ký kiểm toán sử dụng \texttt{ON DELETE SET NULL} để bảo toàn dấu vết khi tài khoản được lưu trữ.
    \item \textbf{Toàn vẹn miền}: Kiểu dữ liệu cột và các ràng buộc \texttt{CHECK} tường minh bảo đảm giá trị thuộc miền hợp lệ: tọa độ nằm trong $[-90, 90]$ và $[-180, 180]$, kích thước và tải trọng dương, phần trăm pin từ $0$ đến $100$.
    \item \textbf{Ràng buộc duy nhất}: Các mã định danh tự nhiên và bản số 1:0..1 được thực thi nghiêm ngặt bằng chỉ mục \texttt{UNIQUE} ở cấp cơ sở dữ liệu.
    \item \textbf{Toàn vẹn nghiệp vụ}: Tổng khối lượng kiện hàng không được vượt quá tải trọng máy bay được phân công (BR-18); địa chỉ phải thuộc về khách hàng đặt đơn (BR-04).
    \item \textbf{Toàn vẹn thời gian}: Trong \texttt{delivery\_activity}, \texttt{started\_at < ended\_at}; trong \texttt{station\_operator\_assignment}, \texttt{assigned\_from < assigned\_to}.
\end{enumerate}

\subsection{Mô hình vòng đời chuyển trạng thái}\label{s:lifecycle}

Hệ thống xác định vòng đời chính thức cho ba thực thể trung tâm có trạng thái:

\paragraph{Vòng đời đơn giao hàng.}
\begin{verbatim}
PENDING
   |
   +---> APPROVED ---> ASSIGNED ---> IN_TRANSIT
   |                                     |
   |                                     +---> DELIVERED (Kết thúc)
   |                                     |
   |                                     +---> FAILED
   |                                              |
   |                                              +---> ASSIGNED (Được lập lịch lại)
   |                                              |
   |                                              +---> CANCELLED (Kết thúc)
   |
   +---> REJECTED (Kết thúc)
   |
   +---> CANCELLED (Kết thúc)
\end{verbatim}

\paragraph{Vòng đời kiện hàng.}
\begin{verbatim}
CREATED -> AT_STATION -> PICKED_UP -> IN_TRANSIT -> DELIVERED
                                         |
                                         +-> FAILED -> RETURNED / DAMAGED
\end{verbatim}

\paragraph{Vòng đời hoạt động giao hàng.}
\begin{verbatim}
ASSIGNED -> PREPARING -> IN_TRANSIT -> DELIVERED / FAILED / CANCELLED
\end{verbatim}

\section{Thiết kế cơ sở dữ liệu mức vật lý}\label{s:physical}

Phần này trình bày chi tiết việc triển khai vật lý bằng PostgreSQL. Tất cả các bảng sử dụng \texttt{BIGINT GENERATED ALWAYS AS IDENTITY} cho khóa chính, \texttt{TIMESTAMPTZ} cho trường thời gian và các miền số, hình học phù hợp.
\subsection{Chiến lược chỉ mục}\label{s:indexes}
Chỉ mục được ưu tiên cho khóa ngoại và các điều kiện lọc truy vấn thường xuyên, đồng thời tránh tạo chỉ mục dư thừa trên những cột đã được bao phủ bởi ràng buộc duy nhất:

\subsection{Các truy vấn nghiệp vụ tiêu biểu}

Các truy vấn sau xác nhận thiết kế vật lý đáp ứng trực tiếp nhu cầu vận hành:

\paragraph{Truy xuất các đơn hàng của khách hàng với trạng thái mới nhất.}
\begin{verbatim}
SELECT order_id, order_status, requested_delivery_time,
       estimated_arrival_at, created_at
FROM   delivery_order
WHERE  customer_id = :customer_id
ORDER  BY created_at DESC;
\end{verbatim}

\paragraph{Truy xuất lịch sử xác nhận lấy kiện hàng tại các trạm.}
\begin{verbatim}
SELECT pse.event_id, pse.package_id, ls.station_name,
       pse.event_type, su.username AS operator_name, pse.occurred_at
FROM   package_station_event pse
JOIN   landing_station ls ON ls.station_id = pse.station_id
LEFT   JOIN app_user su ON su.user_id = pse.performed_by
WHERE  pse.package_id = :package_id
ORDER  BY pse.occurred_at ASC;
\end{verbatim}

\paragraph{Truy vết chuỗi lập lịch lại của một đơn giao hàng.}
\begin{verbatim}
SELECT activity_id, drone_id, activity_status,
       previous_activity_id, started_at, ended_at
FROM   delivery_activity
WHERE  order_id = :order_id
ORDER  BY created_at ASC;
\end{verbatim}

\paragraph{Truy xuất dữ liệu đo từ xa của chuyến bay để giám sát.}
\begin{verbatim}
SELECT recorded_at, latitude, longitude, altitude_m, speed_kmh, battery_level
FROM   tracking_record
WHERE  activity_id = :activity_id
ORDER  BY recorded_at DESC
LIMIT  50;
\end{verbatim}
